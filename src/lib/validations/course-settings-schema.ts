import { z } from "zod";

export const marketingAudienceFormSchema = z.string().trim();
export const ageGroupFormSchema = z.string().trim();
export const courseLevelFormSchema = z.string().trim();
export const deliveryFormatFormSchema = z.string().trim();
export const courseLanguageFormSchema = z.string().trim();
